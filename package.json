{
  "name": "mapbox-gl-shaders",
  "version": "1.0.0",
  "description": "",
  "main": "dist/mapbox-gl-shaders.js",
  "jsnext:main": "dist/mapbox-gl-shaders.es.js",
  "scripts": {
    "build": "mkdir -p dist && node build.js && rollup -i dist/mapbox-gl-shaders.es.js -o dist/mapbox-gl-shaders.js -f cjs",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mapbox/mapbox-gl-shaders.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mapbox/mapbox-gl-shaders/issues"
  },
  "homepage": "https://github.com/mapbox/mapbox-gl-shaders#readme",
  "devDependencies": {
    "rollup": "^0.26.3"
  }
}
